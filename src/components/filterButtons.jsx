import React, { Fragment } from "react";

const filterButtons = () => {
  return (
    <Fragment>
      <section className="filter-button">filter buttons</section>
    </Fragment>
  );
};

export default filterButtons;
